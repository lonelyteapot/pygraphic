"""Date with time (isoformat)"""
scalar DateTime

type Query {
  users: [User!]!
}

scalar UUID

type User {
  id: UUID!
  username: String!
  age: Int!
  balance: Float!
  isActive: Boolean!
  lastSeen: DateTime!
  friends: [User!]!
}